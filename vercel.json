{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/pharmacy build",
  "outputDirectory": "artifacts/pharmacy/dist",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
